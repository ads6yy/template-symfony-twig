{% extends "base.html.twig" %}

{% block title %}{{ 'user.change_password.title'|trans }} - {{ parent() }}{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow">
            <div class="card-header bg-info text-white">
                <h2 class="mb-0">🔐 {{ 'user.change_password.title'|trans }}{% if not require_old_password %} - {{ user.email }}{% endif %}</h2>
            </div>
            <div class="card-body">
                {% if app.flashes %}
                    {% for type, messages in app.flashes %}
                        {% for message in messages %}
                            <div class="alert alert-{{ type }}">
                                {{ message }}
                            </div>
                        {% endfor %}
                    {% endfor %}
                {% endif %}

                {{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}
                    {% if require_old_password %}
                        <div class="mb-3">
                            {{ form_label(form.oldPassword, null, {label_attr: {class: 'form-label'}}) }}
                            {{ form_widget(form.oldPassword, {attr: {class: form.oldPassword.vars.errors|length > 0 ? 'form-control is-invalid' : 'form-control'}}) }}
                            {{ form_help(form.oldPassword) }}
                            {{ form_errors(form.oldPassword) }}
                        </div>
                    {% endif %}

                    <div class="mb-3">
                        {{ form_label(form.newPassword, null, {label_attr: {class: 'form-label'}}) }}
                        {{ form_widget(form.newPassword, {attr: {class: form.newPassword.vars.errors|length > 0 ? 'form-control is-invalid' : 'form-control'}}) }}
                        {{ form_help(form.newPassword) }}
                        {{ form_errors(form.newPassword) }}
                    </div>

                    <div class="mb-3">
                        {{ form_label(form.confirmPassword, null, {label_attr: {class: 'form-label'}}) }}
                        {{ form_widget(form.confirmPassword, {attr: {class: form.confirmPassword.vars.errors|length > 0 ? 'form-control is-invalid' : 'form-control'}}) }}
                        {{ form_help(form.confirmPassword) }}
                        {{ form_errors(form.confirmPassword) }}
                    </div>

                    <input type="hidden" name="_csrf_token" value="{{ csrf_token('change_password') }}">

                    <div class="d-flex gap-2">
                        <button type="submit" class="btn btn-primary">
                            💾 {{ 'user.change_password.submit'|trans }}
                        </button>
                        <a href="{{ path('app_user_show', {id: user.id}) }}" class="btn btn-secondary">
                            ← {{ 'user.change_password.cancel'|trans }}
                        </a>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
